#include <stdio.h>

/**
 * main - prints digit numbers of base ten starting from zero
 *
 * Return: 0 (successful)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");

	return (0);
}
