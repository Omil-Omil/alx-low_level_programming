#include "main.h"
/**
 * _abs - calculates the absolute value of integer
 * @n: the input to check
 * Return: the absolute value the integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
